((lambda ()
    ((define hello "hello")
     (define world " world")
     (define isOk true)
     (define isNot false)
     (if isOk
      then (io.write "isOk")
      else (io.write "else inOk"))
     (if isNot
      then (io.write "else isNot")
      else (io.write "isNot"))
     (io.write hello)
     (io.write " World")
     (define a 123)
     (define b 456)
     (define c (sint64.add a b))
     (io.write c)
     (define c (list.from 1 2 3 4 5 6))
     (io.write c)
     (define add2 (lambda (a b) ((sint64.add a b))))
     (io.write (add2 11 22))
     )
  )
())

{
    (lambda () {
        define let (macro (...) {
            define name  (list.head ...);
            define eqOp (list.head (list.tail ...));
            if (string.eq? (symbol.to_string eqOp) "=")
            then {
                define rest  (list.tail (list.tail ...));
                eval (quote define (unquote name) (unquote rest));
            } else { (); };
        });

        define   h "hello";
        io.write h;       
        io.write "Hello World";
        io.write "Again";
        
        define f (lambda () {
            io.write "I am";
        });
        f();

        define a (let f2 = lambda() {
            io.write "I am 2";
        });

        io.write a;

        let f2 = lambda() {
            io.write "I am 2";
            let a = real64.add -5.3 4.7;
            io.write a; 
        };
        
        f2 ();

        // f2[10][13];
    })()
}